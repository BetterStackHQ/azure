{
  "$schema": "https://schema.management.azure.com/schemas/0.1.2-preview/CreateUIDefinition.MultiVm.json#",
  "handler": "Microsoft.Azure.CreateUIDef",
  "version": "0.1.2-preview",
  "parameters": {
    "config": {
      "isWizard": true,
      "basics": {
        "location": {
          "label": "Region",
          "toolTip": "Azure region for the Event Hub and Function App. Resource logs can only be forwarded from resources in this same region. Metrics and activity logs are collected across all regions regardless.",
          "visible": true
        }
      }
    },
    "basics": [],
    "steps": [
      {
        "name": "deployment",
        "label": "Deployment Settings",
        "elements": [
          {
            "name": "resourceGroupName",
            "type": "Microsoft.Common.TextBox",
            "label": "Resource Group Name",
            "defaultValue": "rg-betterstack",
            "toolTip": "A new resource group will be created with this name to hold the Event Hub and Function App.",
            "constraints": {
              "required": true,
              "regex": "^[-\\w._()]{1,90}$",
              "validationMessage": "Resource group name must be 1-90 characters and can contain alphanumerics, underscores, parentheses, hyphens, and periods."
            }
          }
        ]
      }
    ],
    "outputs": {
      "location": "[location()]",
      "resourceGroupName": "[steps('deployment').resourceGroupName]"
    }
  }
}
